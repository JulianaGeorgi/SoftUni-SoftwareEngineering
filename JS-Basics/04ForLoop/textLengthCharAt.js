function solve(){
    let name = "Todor";

    console.log(name.length - 1); // брои си нормално от 1!!!!!!!!!! за дса вземем последната
    console.log(name[0]); // the string is not an array 

    console.log(name.charAt(5)) // вземане на символ като квадратните скоби и индекса (пак брои от нула), ако не съществува - няма да върне нищо, а другото ще върне undefined 
}

solve();