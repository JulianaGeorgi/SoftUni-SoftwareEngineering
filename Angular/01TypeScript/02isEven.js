// Check if the given number is even or not. Your function should return true if the number is even, and false if the number is odd.
// Input: A number.
// Output: Logic value (boolean).
function isEven(num) {
    return !(num % 2);
}
;
var result2 = isEven(10);
console.log(result2);
